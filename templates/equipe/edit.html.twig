{% extends 'base.html.twig' %}

{% block title %}Edit Equipe{% endblock %}

{% block body %}
    <h1>Edit Equipe</h1>

   <img style="width: 200px; " class="rounded-circle" src="{{ vich_uploader_asset(equipe, 'imageFile') }}"> 
    
    {{ include('equipe/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_equipe_index') }}">back to list</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}


