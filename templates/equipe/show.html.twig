{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>
    {{ dump(equipe) }}

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ equipe.nom }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td> <img style="width: 200px; " class="rounded-circle" src="{{ vich_uploader_asset(equipe, 'imageFile') }}"</td>
            </tr>
            {# <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr> #}
            <tr>
                <th>Categorie</th>
                <td>{{ equipe.categorie }}</td>
            </tr>
            <tr>
                <th>Championnat</th>
                <td>{{ equipe.championnat }}</td>
            </tr>
            <tr>
                <th>Nombre de matchs</th>
                {# <td>{{ dump(equipe.nombreOppositions) }}</td> #}
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_equipe_index') }}">back to list</a>

    <a href="{{ path('app_equipe_edit', {'id': equipe.nom}) }}">edit</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
